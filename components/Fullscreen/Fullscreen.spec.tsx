import React from 'react';
import { render } from '@testing-library/react';
import { Fullscreen } from '.';

describe('Fullscreen', () => {
  test('render', () => {
    render(<Fullscreen />);
  });
});
